services:
  mindshare-frontend:
    build: .
    container_name: mindshare-frontend
    ports:
      - "80:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
